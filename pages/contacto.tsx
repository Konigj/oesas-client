import PrimaryLayout from '../UI/layouts/PrimaryLayout';

const Contacto = () => {
	return (
		<PrimaryLayout pageTitle='Contacto'>
			<main>Home</main>
		</PrimaryLayout>
	);
};

export default Contacto;
