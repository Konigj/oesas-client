import React from 'react';
import PrimaryLayout from '../UI/layouts/PrimaryLayout';

const ErrorPage = () => {
	return (
		<PrimaryLayout pageTitle='Página no encontrada'>
			<main>Home</main>
		</PrimaryLayout>
	);
};

export default ErrorPage;
